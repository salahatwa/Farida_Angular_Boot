import { ServicesFilterPipe } from './services-filter.pipe';

describe('ServicesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ServicesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
